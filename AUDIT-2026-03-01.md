# Superbot2 Drift Audit — 2026-03-01

Comparison of `~/.superbot2/` (runtime) vs `$SUPERBOT2_APP_DIR/` (dev repo).

---

## 1. Templates

| File | Runtime | Dev | Status |
|------|---------|-----|--------|
| `orchestrator-system-prompt-override.md` | Yes | Yes | **Drifted** — runtime has expanded paths (absolute `~/.superbot2/`), dev uses `~/.superbot2/`. This is expected (setup.sh expands). Content was otherwise identical before this session's E2E verification additions. |
| `space-worker-prompt.md` | Yes | **No** | **Runtime-only** — should be in dev |

**Recommendation**: Add `space-worker-prompt.md` to `$SUPERBOT2_APP_DIR/templates/`. It's a template with `{{SPACE}}`, `{{PROJECT}}`, `{{BRIEFING}}`, `{{CODE_DIR}}` placeholders that the orchestrator substitutes at worker spawn time.

---

## 2. Scripts

### Scripts in BOTH (28 files) — mostly identical

| Script | Drifted? | Notes |
|--------|----------|-------|
| `extract-metrics.mjs` | **Yes** | Runtime hardcodes `$HOME/.superbot2`, dev uses `SUPERBOT2_HOME` env var. Also runtime points `CLAUDE_PROJECTS` to `~/.claude/projects/` while dev incorrectly points to `$SUPERBOT_DIR/.claude/projects/`. Dev version is more portable but has a bug in CLAUDE_PROJECTS path. |
| `run-self-improvement.sh` | **Yes** | Runtime hardcodes `$HOME/.superbot2`, dev uses `${SUPERBOT2_HOME:-$HOME/.superbot2}`. Dev is more portable. |
| All others (26 scripts) | No | Identical |

### Scripts ONLY in runtime (not in dev)

| Script | Should be in dev? |
|--------|-------------------|
| `send-imessage.sh` | Already in dev (was a false positive — actually matches) |

### Scripts ONLY in dev (not in runtime)

| Script | Notes |
|--------|-------|
| `install-heartbeat.sh` | Installer utility — correct to be dev-only |
| `install-scheduler.sh` | Installer utility — correct to be dev-only |
| `uninstall-heartbeat.sh` | Uninstaller — correct to be dev-only |
| `uninstall-scheduler.sh` | Uninstaller — correct to be dev-only |
| `parse-activity.mjs` | **Should be synced to runtime** if used at runtime |
| `send-imessage-image.sh` | **Should be synced to runtime** |
| `telegram-watcher.mjs` | **Should be synced to runtime** if used at runtime |

**Recommendation**: Check if `parse-activity.mjs`, `send-imessage-image.sh`, and `telegram-watcher.mjs` are referenced by runtime processes. If so, sync them.

---

## 3. Skills

### Skills in BOTH locations

| Skill | Status |
|-------|--------|
| `domain-finder` | Identical |
| `supercharge-api` | **Drifted** — runtime references "Supercharge Claude Code" marketplace name, dev references "Superbot Marketplace". Dev is newer naming. |

### Skills ONLY in runtime

| Skill | Should be in dev? |
|-------|-------------------|
| `claude-p-streaming` | **No** — ad-hoc/experimental, runtime-only is fine |
| `imessage-setup` | **Maybe** — if it's a stable skill, should be in dev |

### Skills ONLY in dev

| Skill | Notes |
|-------|-------|
| `apple-notes` | Correct — setup.sh deploys to runtime |
| `superbot-brainstorming` | Correct — setup.sh deploys |
| `superbot-browser` | Correct — setup.sh deploys |
| `superbot-implementation` | Correct — setup.sh deploys |
| `systematic-debugging` | Correct — setup.sh deploys |
| `test-driven-development` | Correct — setup.sh deploys |
| `verification-before-completion` | Correct — setup.sh deploys |
| `web-project-setup` | Correct — setup.sh deploys |

**Note**: The dev→runtime deployment happens via `setup.sh`. These skills are in dev but not currently in `~/.superbot2/skills/` because they're deployed as part of the claude code skill system (loaded via `.claude/settings.json` or the agent prompt), not as files in `~/.superbot2/skills/`.

---

## 4. Agents

| Agent | Runtime (`~/.claude/agents/`) | Dev (`$SUPERBOT2_APP_DIR/agents/`) | Status |
|-------|------|-----|--------|
| `code-reviewer.md` | Yes | Yes | Identical |
| `social-media-poster.md` | Yes | Yes | **Drifted** — runtime has inline escalation format with examples; dev references `~/.superbot2/knowledge/escalations.md` instead. Runtime is more self-contained and doesn't depend on an external file. |
| `space-worker.md` | Yes | Yes | Identical |

**Recommendation**: Sync `social-media-poster.md` from runtime to dev. The runtime version is more complete (inline format + examples vs external file reference).

---

## 5. Knowledge Files

### Files in BOTH

| File | Status |
|------|--------|
| `decisions.md` | **Drifted** — runtime has newer entries (skill-creator drafts workflow, 2026-02-28). Dev is behind. |
| `platform-endpoints.md` | **Drifted** — runtime has extra entries (duplicate marketplace endpoints, import/sync plugin endpoints). Dev has slight wording differences. |
| `skill-authoring.md` | **Drifted** — runtime has older format (credentials under `superbot:` key); dev has updated format (credentials at top level, plus `openclaw` dependency system). Dev is newer. |

### Files ONLY in runtime

| File | Should be in dev? |
|------|-------------------|
| `claude-code-expertise.md` | **No** — runtime-only knowledge, orchestrator-managed |
| `conventions.md` | **No** — runtime-only, orchestrator-managed. (Note: per conventions.md, global knowledge is runtime-only) |
| `escalations.md` | **No** — runtime-only |
| `system-prompt-research.md` | **No** — runtime-only |

### Files ONLY in dev

| File | Notes |
|------|-------|
| `preferences.md` | **Stale?** — runtime has `USER.md` for user preferences now. Check if dev `preferences.md` is used. |

**Recommendation**: Per the conventions doc, global `~/.superbot2/knowledge/` files are runtime-only and should NOT be synced to dev. The dev `knowledge/` directory should only contain files that are part of the superbot2 system (not user/orchestrator-generated knowledge). The three files in dev (`decisions.md`, `platform-endpoints.md`, `skill-authoring.md`) appear to be stale dev copies that aren't part of the deployment pipeline.

---

## 6. Guides (ORCHESTRATOR_GUIDE.md, SPACE_WORKER_GUIDE.md)

| File | Runtime | Dev | Status |
|------|---------|-----|--------|
| `ORCHESTRATOR_GUIDE.md` | `~/.superbot2/ORCHESTRATOR_GUIDE.md` | `$SUPERBOT2_APP_DIR/ORCHESTRATOR_GUIDE.md` (root) | **Identical** (before this session's E2E changes). Dev copy is at repo root, NOT in templates/. |
| `SPACE_WORKER_GUIDE.md` | `~/.superbot2/SPACE_WORKER_GUIDE.md` | **Missing** | **Runtime-only — should be in dev** |

The SPACE_WORKER_GUIDE.md's own sync table says it should be at `$SUPERBOT2_APP_DIR/templates/SPACE_WORKER_GUIDE.md`, but that file doesn't exist.

**Recommendation**: Copy `SPACE_WORKER_GUIDE.md` to dev repo. Either at root (matching ORCHESTRATOR_GUIDE.md pattern) or in `templates/` (matching the guide's own sync instructions). Root is more consistent with current ORCHESTRATOR_GUIDE.md placement.

---

## 7. Runtime-Only Files (Expected)

These files exist only in `~/.superbot2/` and correctly should NOT be in dev:

| File | Purpose |
|------|---------|
| `IDENTITY.md` | Superbot2 persona identity |
| `USER.md` | User preferences and profile |
| `MEMORY.md` | Orchestrator session memory |
| `auto-triage-rules.jsonl` | User-defined escalation auto-resolve rules (currently empty) |
| `config.json` | Runtime config (schedule, settings) |
| `disabled-hooks.json` | (Not found — may not exist yet) |
| `spaces/` | All space data, plans, tasks |
| `escalations/` | Escalation files |
| `sessions/` | Session history |
| `todos.json` | User todos |
| `skill-creator/drafts/` | Draft skills awaiting review |

---

## 8. Hooks

| Location | Files |
|----------|-------|
| Runtime (`~/.superbot2/hooks/`) | `pre-compact.sh`, `pre-shutdown.sh`, `subagentstop-log.sh`, `task-completed.sh`, `teammate-idle.sh` |
| Dev (`$SUPERBOT2_APP_DIR/hooks/`) | **Empty directory / doesn't exist** |

Per meta knowledge: "teammate-idle.sh hook lives only at `~/.superbot2/hooks/` (runtime). There is no copy in `$SUPERBOT2_APP_DIR/hooks/`."

**Recommendation**: Hooks should be in dev for version control. These are critical system behavior. Copy all 5 hooks to `$SUPERBOT2_APP_DIR/hooks/`. The hooks reference `~/.superbot2/` paths, which is fine — they run at runtime.

---

## 9. Summary of Actions Needed

### Must sync (critical)

1. **`SPACE_WORKER_GUIDE.md`** → copy runtime to `$SUPERBOT2_APP_DIR/SPACE_WORKER_GUIDE.md`
2. **`templates/space-worker-prompt.md`** → copy runtime to `$SUPERBOT2_APP_DIR/templates/space-worker-prompt.md`
3. **`agents/social-media-poster.md`** → copy runtime to `$SUPERBOT2_APP_DIR/agents/social-media-poster.md`
4. **`hooks/*`** → copy all 5 hooks from runtime to `$SUPERBOT2_APP_DIR/hooks/`

### Should sync (important)

5. **`scripts/extract-metrics.mjs`** — runtime is behind (no SUPERBOT2_HOME support). But dev has a CLAUDE_PROJECTS path bug. Needs manual merge.
6. **`scripts/run-self-improvement.sh`** — runtime is behind (no SUPERBOT2_HOME support). Dev version is better.
7. **`skills/supercharge-api`** — minor naming difference. Dev has newer naming.

### Consider (low priority)

8. Dev `knowledge/` files may be stale — `decisions.md`, `platform-endpoints.md`, `skill-authoring.md` are drifted from runtime but global knowledge is supposed to be runtime-only per conventions.
9. Dev-only scripts (`parse-activity.mjs`, `send-imessage-image.sh`, `telegram-watcher.mjs`) — check if they should be deployed to runtime.
10. Dev `knowledge/preferences.md` may be obsolete (replaced by `USER.md` in runtime).

---

## 10. Conventions Doc Accuracy

The `~/.superbot2/knowledge/conventions.md` sync table is partially outdated:

- States `ORCHESTRATOR_GUIDE.md` and `SPACE_WORKER_GUIDE.md` are "runtime-only (no dev counterpart)" — but ORCHESTRATOR_GUIDE.md IS in dev (at repo root). And SPACE_WORKER_GUIDE.md's own file says it should be synced to dev.
- Missing entries for: `hooks/`, `space-worker-prompt.md` template
- Should mention that `ORCHESTRATOR_GUIDE.md` lives at dev repo root (not in templates/)
